<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/signup.css">
    <title>Movers Log In</title>
</head>
<body>
    <div class="container">
        <div class="illustration">
            <div class="image-container">
                <img src="/images/login.svg" alt="SVG ILLUSTRATION">
            </div>
        </div>
        <div class="sign-up-form">
            <form action="http://localhost:3000/app/mover/login" method="post">
                <h1>LOGIN AS A RELOCATOR</h1>
                <div class="input-box">
                    <label for="email">Email</label>
                    <input type="email" name="email" id="email" placeholder="someone@company.com" required>
                </div>
                <div class="input-box">
                    <label for="password">Password</label>
                    <input type="password" name="password" id="password" placeholder="********" required>
                </div>
                <button class="btn submit">Submit</button>
            </form>
        </div>
    </div>
    <div class="errors-container">
        <% if (errors) {%>
            <p>There are errors in the form!</p>
            <hr>
            <ul>
                <% errors.forEach(error => { %>
                    <li><%= error %></li>
                <% }) %>
        <% } %>
    </div>
</body>
</html>